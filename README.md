# PacBot Game Simulator
We use this to simulate the physical PacBot robot for use in testing everything.

### To set up and run:
In your terminal:
```bash
python -m venv venv
source ./venv/bin/activate
pip install -r requirements.txt
```